version: "1.1.5"
cache: true

interface:
  codeExecution: true
  htmlArtifacts: true
  reactArtifacts: true

webSearch:
  searchProvider: "tavily"
  tavilyApiKey: "${TAVILY_API_KEY}"
  scraperProvider: "tavily"
  rerankerType: "none"

fileStrategies:
  avatar: "local"
  image: "local"
  document: "s3"
